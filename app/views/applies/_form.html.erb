<h2>Inscrição</h2>

<%= form_for apply do |f| %>
    <div class="form-group">
      <%= f.label :applicant_name, 'Nome' %>
      <%= f.text_field :applicant_name %>
    </div>
    <div class="form-group">
      <%= f.label :email, 'Email' %>
      <%= f.text_field :email %>
    </div>
    <div class="form-group">
      <%= f.label :job_id, 'Vagas' %>
      <%= f.collection_select(:job_id, @jobs, :id, :title)  %>
    </div>
      <%= f.submit 'Enviar' %>
<% end %>
  