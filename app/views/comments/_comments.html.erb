<h3>Comentários</h3>

<% @proposal.comments.each do |comment| %>
  <%= comment.username %>
  <%= comment.body %>

  <p><%= link_to 'Deletar', [comment.proposal, comment], method: :delete,
                            data: {confirm: 'Tem certeza?'}  %></p>
<% end %>