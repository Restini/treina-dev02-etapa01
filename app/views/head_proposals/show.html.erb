<h2><%= @head_proposal.title %></h2>
<p><%= @head_proposal.body %></p>

<p><%= link_to 'Editar', edit_head_proposal_path(@head_proposal) %></p>
<p><%= link_to 'Deletar', head_proposal_path(@head_proposal), method: :delete,
                            data: {confirm: 'Tem certeza?'}  %></p>

<%= render 'head_comments/comments' %>
<%= render 'head_comments/form' %>

<p><%= link_to 'Voltar', head_profiles_path %>