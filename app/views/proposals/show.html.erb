<h2><%= @proposal.title %></h2>
<p><%= @proposal.body %></p>

<p><%= link_to 'Editar', edit_proposal_path(@proposal) %></p>
<p><%= link_to 'Deletar', proposal_path(@proposal), method: :delete,
                            data: {confirm: 'Tem certeza?'}  %></p>

<%= render 'comments/comments' %>
<%= render 'comments/form' %>

<p><%= link_to 'Voltar', head_profiles_path %>
